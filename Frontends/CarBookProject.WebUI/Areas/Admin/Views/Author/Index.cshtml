@model List<ResultAuthorDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">

        <div class="row">
            <div class="col-12">
                <div class="page-title-box d-flex align-items-center justify-content-between">
                    <h4 class="mb-0 font-size-18">Yazarlar Listesi</h4>

                    <div class="page-title-right">
                        <ol class="breadcrumb m-0">
                            <li class="breadcrumb-item"><a href="javascript: void(0);">Pages</a></li>
                            <li class="breadcrumb-item active">Yazarlar</li>
                        </ol>
                    </div>
                                
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <table id="basic-form" class="table dt-responsive nowrap">
                            <h4 class="card-title">Yazarların Bilgileri</h4>

                            <thead>
                                <tr>
                                    <th>Görsel</th>
                                    <th>Yazar Adı</th>
                                    <th>Açıklama</th>
                                    <th>Güncelle</th>
                                    <th>Sil</th>
                                </tr>
                            </thead>
                                    
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><img src="@item.AuthorImageUrl" width="50px;" height="50px" /></td>
                                        <td>@item.AuthorName</td>
                                        <td>@item.AuthorDescription</td>
                                        <td><a class="btn btn-success btn-rounded" href="/Admin/Author/UpdateAuthor/@item.AuthorId">Güncelle</a></td>
                                        <td><a class="btn btn-danger btn-rounded" href="/Admin/Author/RemoveAuthor/@item.AuthorId">Sil</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>









